import React from 'react';

import { Text } from '@ui';
import Root from './cycle-picker.styles';

const CyclePicker = () => {
	return (
		<Root>
			<Text>Cycle Picker</Text>
		</Root>
	);
};

export default CyclePicker;

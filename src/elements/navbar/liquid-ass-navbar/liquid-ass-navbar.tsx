import React from 'react';
import { View, Text } from 'react-native';

const LiquidAssNavbar = () => {
	return (
		<View>
			<Text>LiquidAssNavbar</Text>
		</View>
	);
};

export default LiquidAssNavbar;

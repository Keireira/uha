// import { Platform } from 'react-native';

import OlderIOSNavbar from './older-ios-navbar';
// import LiquidAssNavbar from './liquid-ass-navbar';

// const isiOS26 = Platform.OS === 'ios' && Number.parseInt(`${Platform.Version}`, 10) >= 26;

// const Navbar = isiOS26 ? LiquidAssNavbar : OlderIOSNavbar;

export default OlderIOSNavbar;
